# This entire file is a systemd credential. Store it root:root mode 0600.
# Long-lived R2 access keys are deliberately forbidden here. The control plane
# issues prefix-scoped temporary credentials after each successful mTLS registration.
RESTIC_PASSWORD=REPLACE_WITH_A_LONG_RANDOM_REPOSITORY_PASSWORD
AWS_DEFAULT_REGION=auto
